export enum UserRole {
  READER = 'reader',
  AUTHOR = 'author',
  MODERATOR = 'moderator',
  ADMIN = 'admin'
}

export enum ArticleStatus {
  DRAFT = 'draft',
  PUBLISHED = 'published',
  ARCHIVED = 'archived'
}
